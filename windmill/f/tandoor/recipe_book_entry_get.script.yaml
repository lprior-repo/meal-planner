summary: Get a recipe book entry
description: Gets a recipe book entry by ID.
kind: script
language: bash
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties:
    entry_id:
      type: integer
      description: Entry ID
    tandoor:
      type: string
      description: Tandoor API credentials
  required:
    - tandoor
    - entry_id
