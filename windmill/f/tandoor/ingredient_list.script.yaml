summary: List ingredients
description: Lists ingredients from Tandoor with optional pagination
lock: ''
kind: script
language: bash
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties:
    page:
      type: string
      description: Page number (optional)
      default: null
      originalType: string
    page_size:
      type: string
      description: Number of items per page (optional)
      default: null
      originalType: string
    tandoor:
      type: string
      description: Tandoor API credentials
      default: null
      originalType: string
  required:
    - tandoor
    - page
    - page_size
