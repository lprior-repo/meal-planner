name = "shared"
version = "1.0.0"

description = "Shared types for meal planner frontend and backend"
licences = ["MIT"]

# Works on both JavaScript (client) and Erlang (server)
target = "erlang"

gleam = ">= 1.0.0"

[dependencies]
gleam_stdlib = ">= 0.65.0"
gleam_json = ">= 3.0.0 and < 4.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
