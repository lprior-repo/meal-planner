---
doc_id: ops/commands/project-graph
chunk_id: ops/commands/project-graph#chunk-1
heading_path: ["project-graph"]
chunk_type: prose
tokens: 84
summary: "project-graph"
---

# project-graph

> **Context**: The `moon project-graph [id]` (or `moon pg`) command will generate and serve a visual graph of all configured projects as nodes, with dependencies bet

The `moon project-graph [id]` (or `moon pg`) command will generate and serve a visual graph of all configured projects as nodes, with dependencies between as edges, and can also output the graph in [Graphviz DOT format](https://graphviz.org/doc/info/lang.html).

```
