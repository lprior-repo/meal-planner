# Type Alias: CheckID

> **CheckID** = `string` & `object`

The `CheckID` scalar type represents an identifier for an object of type Check.

## Type Declaration

### __CheckID

> **__CheckID**: `never`
