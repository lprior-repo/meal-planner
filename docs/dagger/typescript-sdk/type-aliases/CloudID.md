# Type Alias: CloudID

> **CloudID** = `string` & `object`

The `CloudID` scalar type represents an identifier for an object of type Cloud.

## Type Declaration

### __CloudID

> **__CloudID**: `never`
