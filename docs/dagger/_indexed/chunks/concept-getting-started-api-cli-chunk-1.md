---
doc_id: concept/getting-started/api-cli
chunk_id: concept/getting-started/api-cli#chunk-1
heading_path: ["api-cli"]
chunk_type: prose
tokens: 103
summary: "> **Context**: The Dagger CLI lets you call both the core and extended Dagger API (the core APIs ..."
---
# Using the Dagger CLI

> **Context**: The Dagger CLI lets you call both the core and extended Dagger API (the core APIs plus the new APIs provided by external Dagger modules) directly from...


The Dagger CLI lets you call both the core and extended Dagger API (the core APIs plus the new APIs provided by external Dagger modules) directly from the command-line.

You can call the API interactively (`dagger`) or non-interactively (`dagger -c`, `dagger call`, or `dagger core`).
