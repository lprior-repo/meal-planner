---
doc_id: ops/getting-started/installation
chunk_id: ops/getting-started/installation#chunk-2
heading_path: ["installation", "Stable release"]
chunk_type: mixed
tokens: 58
summary: "Install the latest stable release of the Dagger CLI following the steps below."
---
Install the latest stable release of the Dagger CLI following the steps below.

### macOS

We assume that you have [Homebrew](https://brew.sh/) installed. If you do, you can install `dagger` with a single command:

```bash
brew install dagger/tap/dagger
```

This installs `dagger` in:

```bash
type dagger
