---
doc_id: ref/type-aliases/platform
chunk_id: ref/type-aliases/platform#chunk-1
heading_path: ["platform"]
chunk_type: prose
tokens: 46
summary: "> **Context**: > **Platform** = `string` & `object`


> **Platform** = `string` & `object`

The p..."
---
# Type Alias: Platform

> **Context**: > **Platform** = `string` & `object`


> **Platform** = `string` & `object`

The platform config OS and architecture in a Container.

The format is [os]/[platform]/[version] (e.g., "darwin/arm64/v7", "windows/amd64", "linux/arm64").
