---
doc_id: ops/reference/container-runtimes-apple-container
chunk_id: ops/reference/container-runtimes-apple-container#chunk-2
heading_path: ["container-runtimes-apple-container", "Prerequisites"]
chunk_type: mixed
tokens: 27
summary: "Ensure that `container` is installed on your Mac:

```bash
container system start
```

Install `c..."
---
Ensure that `container` is installed on your Mac:

```bash
container system start
```

Install `container` using the official instructions at [github.com/apple/container](https://github.com/apple/container).
