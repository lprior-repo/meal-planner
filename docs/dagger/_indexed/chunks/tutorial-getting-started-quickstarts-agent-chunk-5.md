---
doc_id: tutorial/getting-started/quickstarts-agent
chunk_id: tutorial/getting-started/quickstarts-agent#chunk-5
heading_path: ["quickstarts-agent", "Initialize a Dagger module"]
chunk_type: code
tokens: 53
summary: "In an empty directory, create a new Dagger module:

**Go:**
```bash
dagger init --sdk=go --name=codi"
---
In an empty directory, create a new Dagger module:

**Go:**
```bash
dagger init --sdk=go --name=coding-agent
```

**Python:**
```bash
dagger init --sdk=python --name=coding-agent
```

**TypeScript:**
```bash
dagger init --sdk=typescript --name=coding-agent
```

To see the generated Dagger Functions, run:

```bash
dagger functions
```
