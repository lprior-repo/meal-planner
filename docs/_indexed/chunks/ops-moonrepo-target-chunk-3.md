---
doc_id: ops/moonrepo/target
chunk_id: ops/moonrepo/target#chunk-3
heading_path: ["Targets", "Run `lint` in project `app`"]
chunk_type: prose
tokens: 62
summary: "Run `lint` in project `app`"
---

## Run `lint` in project `app`
$ moon run app:lint
```

### By tag (v1.4.0)

Another way to target projects is with the tag scope, which requires the name of a tag prefixed with `#`, and will run a specific task in all projects with that tag.

```bash
