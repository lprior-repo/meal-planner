---
doc_id: ops/windmill/rust-client
chunk_id: ops/windmill/rust-client#chunk-2
heading_path: ["Rust client", "Installation"]
chunk_type: code
tokens: 42
summary: "Installation"
---

## Installation

To use the Rust client library, you need to add the `wmill` crate to your `Cargo.toml` dependencies:

```toml
[dependencies]
wmill = "^1.0"
```

Or using cargo:

```bash
cargo add wmill
```
