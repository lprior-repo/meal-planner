# https://moonrepo.dev/docs/config/toolchain
$schema: './cache/schemas/toolchain.json'

# Enable Rust language support
# This enables:
# - Cargo.lock parsing for accurate dependency hashing
# - Cargo binary resolution (~/.cargo/bin)
# - Rust-specific task automations
# - Lockfile generation if missing
#
# Version is managed by mise (see mise.toml), not moon
rust: {}
